---
// This component is a mess please don't judge me lmao

---

<html>
  <div class="container">
    <slot />

    <style>
      .container {
        display: grid;
        height: fit-content;
        grid-template-columns: 1fr 1fr;
        margin-top: 2rem;
        margin-bottom: 2rem;

        padding: 2rem;

      }

      /* Colaspe components on top of each other if using mobile layout */
      @media only screen and (max-width: 900px) {
        .container {
          grid-template-columns: 1fr;
          height: fit-content;
          
          
        }
      }
      .container :global(.image) {
        display: flex;
        justify-content: center;
      }

      .container :global(.image .image_container) {
        width: 70%;
        height: 70%;
        display: block;

        align-self: center;

        border-radius: 18px;
      }

      .container :global(.image p) {
        font-family: "Atkinson Hyperlegible", sans-serif;
        font-weight: 400;
        font-style: normal;
      }

      .container :global(.image .image_container img) {
        width: 100%;
        height: 100%;
        display: flex;
        margin: auto;

        border: 1px solid #dce1e9;

        border-radius: 18px;
        object-fit: cover;
      }
      .container :global(.description) {
        display: flex;
        justify-content: center;
      }
      .container :global(.description .text_wrapper) {
        margin: 4rem;
      }
      .container :global(.description .text_wrapper h1) {
        font-family: "Atkinson Hyperlegible", sans-serif;
        font-weight: 600;
        font-style: normal;
      }
      .container :global(.description .text_wrapper p) {
        font-family: "Atkinson Hyperlegible", sans-serif;
        font-weight: 400;
        font-style: normal;
      }
    </style>
  </div>


</html>
